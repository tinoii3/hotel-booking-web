info:
  name: bookings
  type: http
  seq: 6

http:
  method: GET
  url: "{{localhost}}/api/bookings/booking"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
