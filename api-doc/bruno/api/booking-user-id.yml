info:
  name: booking-user-id
  type: http
  seq: 11

http:
  method: GET
  url: "{{localhost}}/api/bookings/booking/:user_id"
  params:
    - name: user_id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
