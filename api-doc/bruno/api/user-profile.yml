info:
  name: user-profile
  type: http
  seq: 11

http:
  method: GET
  url: "{{localhost}}/api/auth/user-profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
