info:
  name: rever
  type: http
  seq: 12

http:
  method: GET
  url: "{{localhost}}/api/reversation/reservations"
  auth:
    type: bearer
    token: admin_456

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
