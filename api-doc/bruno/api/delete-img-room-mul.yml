info:
  name: delete-img-room-mul
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{localhost}}/api/upload/delete-multiple"
  body:
    type: json
    data: |-
      {
        "publicIds": [
          "hotel/rooms/o3u2n7pe9a4nmqzygusl"
        ]
      }
  auth:
    type: bearer
    token: admin_456

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
