info:
  name: room-type-info
  type: http
  seq: 9

http:
  method: GET
  url: "{{localhost}}/api/home/room-types-with-cover"
  auth:
    type: bearer
    token: admin_456

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
