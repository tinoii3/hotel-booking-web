info:
  name: room-type-info
  type: http
  seq: 10

http:
  method: GET
  url: "{{localhost}}/api/manage-room/room-types-with-cover"
  auth:
    type: bearer
    token: admin_456

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
